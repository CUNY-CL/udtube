seed_everything: 42
trainer:
    max_epochs: 2
data:
  model_dir: records/tmp
  train: datasets/toy_datasets/en_ewt-ud-toy.conllu
  val: datasets/toy_datasets/en_ewt-ud-toy.conllu
  test: datasets/toy_datasets/en_ewt-ud-toy.conllu
  predict: datasets/toy_datasets/en_ewt-ud-toy.conllu
  batch_size: 8
checkpoint:
  filename: "model-{epoch:03d}-{val_loss:.4f}"
  monitor: val_loss
  verbose: true
# prediction.path is missing as it is set when called!